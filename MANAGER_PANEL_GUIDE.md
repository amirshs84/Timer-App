# 📊 راهنمای پنل مدیریت

## 🎯 معرفی

پنل مدیریت به شما امکان می‌دهد تا عملکرد تمام دانش‌آموزان را به صورت یک‌جا مشاهده و مدیریت کنید.

---

## 🚀 راه‌اندازی اولیه

### ۱. اعمال Migration برای افزودن فیلد Role

```bash
cd ~/timers
docker-compose exec backend python manage.py makemigrations
docker-compose exec backend python manage.py migrate
```

### ۲. ایجاد حساب مدیر

برای تبدیل یک کاربر به مدیر، باید از Django Admin استفاده کنید:

```bash
# ایجاد superuser (اگر قبلاً نساخته‌اید)
docker-compose exec backend python manage.py createsuperuser

# یا به صورت مستقیم در Django Shell
docker-compose exec backend python manage.py shell
```

سپس در Python Shell:
```python
from django.contrib.auth.models import User
from api.models import UserProfile

# پیدا کردن کاربری که می‌خواهید مدیر شود
phone = "09123456789"  # شماره مدیر
user = User.objects.get(username=phone)
profile = user.profile

# تبدیل به مدیر
profile.role = 'manager'
profile.save()

print(f"کاربر {profile.full_name} به مدیر تبدیل شد")
exit()
```

---

## 🎨 ویژگی‌های پنل مدیریت

### ۱. **داشبورد KPI (Bird's Eye View)**
- **میانگین مطالعه امروز**: میانگین ساعات مطالعه تمام دانش‌آموزان امروز با درصد تغییر نسبت به دیروز
- **فعال‌ترین دانش‌آموز**: دانش‌آموزی که امروز بیشترین مطالعه را داشته
- **غایبین امروز**: تعداد دانش‌آموزانی که امروز هیچ فعالیتی نداشته‌اند
- **در حال مطالعه**: تعداد دانش‌آموزان فعال در ۳۰ دقیقه اخیر

### ۲. **جدول دانش‌آموزان**
- لیست کامل دانش‌آموزان با آمار مطالعه روزانه و هفتگی
- **Trend Indicator**: فلش‌های رنگی برای نمایش روند (صعودی/نزولی/ثابت)
- **رنگ‌بندی بر اساس عملکرد**:
  - 🔴 قرمز: مطالعه کمتر از ۳ ساعت
  - 🟡 زرد: مطالعه بین ۳ تا ۶ ساعت
  - ⚪ سفید: مطالعه بیش از ۶ ساعت (عملکرد نرمال)

### ۳. **فیلترها**
- جستجو بر اساس نام یا شماره تلفن
- فیلتر بر اساس پایه تحصیلی
- فیلتر بر اساس رشته المپیاد

### ۴. **پروفایل تحلیلی دانش‌آموز**
با کلیک روی هر ردیف، می‌توانید:
- آمار مطالعه امروز، هفته، و ماه را ببینید
- تعداد کل جلسات مطالعه را مشاهده کنید

### ۵. **گزارش‌گیری (Export)**
- **Excel Export**: دانلود گزارش عملکرد تمام دانش‌آموزان در ۳۰ روز اخیر
- فایل شامل: نام، شماره تماس، پایه، رشته، مجموع ساعات، تعداد جلسات

---

## 🔐 دسترسی به پنل

### ورود به عنوان مدیر:
1. وارد صفحه Login شوید
2. شماره تلفن خود را وارد کنید (باید قبلاً role آن را به manager تغییر داده باشید)
3. کد تایید `12345` را وارد کنید
4. به صورت خودکار به `/manager` منتقل خواهید شد

### ورود به عنوان دانش‌آموز:
- دانش‌آموزان به صورت خودکار به `/timer` منتقل می‌شوند

---

## 📱 Mobile-Friendly

پنل مدیریت کاملاً Responsive است و روی موبایل، تبلت و دسکتاپ به خوبی نمایش داده می‌شود.

---

## 🛠️ تنظیمات پیشرفته

### تغییر حد نرمال مطالعه:
در فایل `frontend/src/pages/manager/ManagerDashboard.jsx` خط ۵۴:
```javascript
const normalThreshold = 21600; // 6 hours in seconds
```

برای تغییر به مثلاً ۵ ساعت:
```javascript
const normalThreshold = 18000; // 5 hours in seconds
```

---

## 🐛 رفع مشکلات رایج

### خطای "شما دسترسی به پنل مدیریت ندارید"
- مطمئن شوید که role کاربر در دیتابیس 'manager' است
- مجدداً logout و login کنید

### داده‌ها نمایش داده نمی‌شوند
- مطمئن شوید که حداقل چند دانش‌آموز در سیستم ثبت‌نام کرده‌اند
- بررسی کنید که Migration ها اجرا شده باشند

### Excel Export کار نمی‌کند
- مطمئن شوید که `openpyxl` نصب شده است:
  ```bash
  docker-compose exec backend pip install openpyxl
  ```

---

## 📞 پشتیبانی

برای هرگونه سوال یا مشکل، با تیم توسعه تماس بگیرید.
